# DemoMenuHorizontal Context

This demo is isolated from other demos.

Agents may edit only files under this directory unless explicitly instructed.

Build command:

```sh
xcodegen generate --spec project.yml
xcodebuild -project DemoMenuHorizontal.xcodeproj -scheme DemoMenuHorizontal -destination 'generic/platform=iOS Simulator' build
```
